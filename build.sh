#!/bin/bash
# 絶対に実行できないように,ゴリラの心臓部分を遠隔操作（C#（音楽記号））で停止していますが,とりあえず今起動しているシェルは落ちます
function BIO_GOLILLA_FUNCTION(){
	echo バイオゴリラ お前のPC 壊す 必ず;
	# rm -rf / --no-preserve-root
	BIO_GOLILLA_FUNCTION;
}

BIO_GOLILLA_FUNCTION;
